## Set up the dev envirenement

### Generate the database

Run this command in the backend folder
`npx prisma migrate dev`

### Run the backend server

Run this command in the backend folder
`npm run start:dev`

### Run the frontend server

Run this command in the frontend folder
`npm run dev`
